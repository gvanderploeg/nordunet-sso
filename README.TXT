To install this plugin, copy the generated jar file to the %crowd-home%/plugins directory and restart Crowd

To install during development on crowd plugins (having a running Crowd instance), run from within this module:
# atlas-install-plugin
